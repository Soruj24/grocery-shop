/**
 * @deprecated Import `Modal` / `Dialog` from `@/components/ui` instead.
 *
 * This file is kept for backward compatibility and delegates to the
 * system Modal (focus trap, Escape handling, overlay token, sizes).
 * Props (`isOpen`, `onClose`, `title`, `children`, `size`) are compatible.
 */
export { Modal as default } from "./system/Modal";
